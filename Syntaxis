function lowerToUpperCase(input) {

     let char = input[0];
     
     if(char === char.toUpperCase()){
        console.log("upper-case");
        }else if(char === char.toLowerCase()){
        console.log("lower-case");
        
        }
        
}
